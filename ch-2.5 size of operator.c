//size of operator//
#include<stdio.h>
int main()
{
    int i;
    float k;
    char ch;
    double d;

    printf("size of integer = %d\n",sizeof(i));
    printf("size of float = %d\n",sizeof(k));
    printf("size of char = %d\n",sizeof(ch));
    printf("size of double = %d\n",sizeof(d));

    return 0;
}
